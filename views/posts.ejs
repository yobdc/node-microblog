<div class="container">
  <% posts.forEach(function(post, index) { %>
    <% if(index%3==0){ %>
      <div class="row">
    <% } %>
    <div class="col-md-4">
      <h2><a href="/u/<%= post.username%>"><%= post.username%></a> 说</h2>
      <p><%=post.post%></p>
    </div>
    <% if(index%3==2){ %>
      </div>
    <% } %>
  <% }) %>
  </div>
</div>